---
- name: Stop VirtualBox VMs
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    vms:
      - ubuntu1
      - kali-linux-2025.4-virtualbox-amd64

  tasks:
    - name: Get list of running VMs
      ansible.builtin.command: VBoxManage list runningvms
      register: running_vms
      changed_when: false

    - name: Gracefully shut down running VMs
      ansible.builtin.command: VBoxManage controlvm "{{ item }}" acpipowerbutton
      loop: "{{ vms }}"
      when: item in running_vms.stdout
